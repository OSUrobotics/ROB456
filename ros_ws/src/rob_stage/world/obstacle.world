#!/usr/bin/env python3


# Bill Smart, smartw@oregonstate.edu


include "include/pioneer.inc"
include "include/map.inc"
include "include/sick.inc"
include "include/box.inc"

# spatial resolution of the underlying occupancy grid. Default is 0.02m.
resolution 0.02

# set the multiple of real time that Stage should try to achieve. Default is 1. 
# set to 0 to go as fast as possible.
speedup 1

# configure the GUI window
window
(
  size [ 635.000 666.000 ] # in pixels
  scale 36.995   # pixels per meter
  center [ -0.040  -0.274 ]
  rotate [ 0  0 ]
  			
  show_data 1              # 1=on 0=off
)

# load an environment bitmap
floorplan
( 
  name "rink"
  size [16.000 16.000 0.800]
  pose [0 0 0 0]
  bitmap "bitmaps/rink.png"
)


pioneer2dx_base_no_sonar
(		  
  # can refer to the robot by this name
  name "robot"
  pose [ -2 0 0 30 ] 

  sicklaser( pose [ 0 0 0 0 ] ) 
 
  trail_length 400
)

box(name "box1" pose [ 0 -5 0 0 ])
box(name "box2" pose [ 0 5 0 0 ])
box(name "box3" pose [ 5.5 0 0 0 ])
box(name "box3" pose [ -5 0 0 0 ])
box(name "box4" pose [ 1 -0.25 0 0 ])
box(name "box5" pose [ 3 0.5 0 0])
